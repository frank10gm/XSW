<?xml version="1.0" encoding="UTF-8"?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms" xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml" x:Class="StritWalk.NewItemPage" Title="New Item">
	<ContentPage.ToolbarItems>
		<ToolbarItem Text="Save" Clicked="Save_Clicked" />
	</ContentPage.ToolbarItems>
    <ContentPage.Content>
        <StackLayout Spacing="20" Padding="15" x:Name="mainLayout2">
            <Label Text="Title" FontSize="Medium" />
			<Entry Text="{Binding Item.Name}" FontSize="Small" />
			<Label Text="Description" FontSize="Medium" />
            <Editor Text="{Binding Item.Description}" FontSize="Small" Margin="0" />
            <!--Due date-->
            <Label Text="Due Date" FontSize="Medium" />
            <Entry Text="Dec 9, 2099" FontSize="Small" />
            <!--To Do List-->
            <Label Text="To-Do" FontSize="Medium" />
            <Entry Text="Step 1" FontSize="Small" Margin="0" />
            <Button Text="+ step" FontSize="Small" Margin="0" />
        </StackLayout>
	</ContentPage.Content>
</ContentPage>
